<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.text_field :title %>
  <%= f.text_field :artist_name %>
  <%= f.collection_select :genre_id, Genre.all, :name %>
  <input name="song[note_ids][]">
  <input name="song[note_ids][]">
  <input name="song[note_ids][]">

  <%= f.submit %>
<% end %>
